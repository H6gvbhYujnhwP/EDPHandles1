# COSMA Express Image Update Verification Note

The COSMA EXPRESS RANGE page was checked locally after the requested image updates.

## Verified results

| Area | Verified result |
|---|---|
| First page image | The top page image now uses `/images/cosma-express/cosma-express-hero-replacement.png`, which is the newly provided replacement asset. |
| Reused original image | The displaced original first image `/images/cosma-express/cosma-express-hero.jpg` is now assigned as the second image in the lower image pair associated with the COSMA Express range content. |

The page also passed `pnpm check && pnpm build` after the changes.
