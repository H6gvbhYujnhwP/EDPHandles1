# Route Recovery Notes

The published domain at `https://edphandles-frvbfq3j.manus.space/` is still showing the previously published state, where the in-progress editorial handles build appears on the root URL. This indicates the live domain has not yet been updated to the latest routing changes.

The development preview at `https://3000-ikreuuosaonggvrtkbehw-07095ce7.us1.manus.computer/` now shows the recovered homepage on `/`. The restored root page contains the earlier marketing homepage with the headline "Italian Style to the UK" and the hero CTA linking to the handles area.

This confirms the route recovery is working in the current project state. The remaining validation step is to confirm that the development preview also serves the in-progress editorial build correctly on `/handles`.
